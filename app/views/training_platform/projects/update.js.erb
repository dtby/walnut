<% if @project.errors.blank? %>
  location.href = "<%= training_platform_root_path%>";
<% else %>
  $("#trainingPlatformModalContent").html("<%= escape_javascript(render 'training_platform/projects/form') %>");
  $("#trainingPlatformModal").modal('show');
<% end %>