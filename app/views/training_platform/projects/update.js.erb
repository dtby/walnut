<% if @project.errors.blank? %>
  location.href = "<%= training_platform_root_path%>";
<% else %>
  $("#deviseUserFormModal").html("<%= escape_javascript(render 'training_platform/projects/form') %>");
  $("#deviseUserModal").modal('show');
<% end %>